# Digial_Output

This program allows the execution of different modes (blinking, alternating, and running) for a set of LED lights connected to the GPIO32, GPIO33, GPIO25, GPIO26, GPIO27, GPIO14 pins with a 1-second interval per cycle.